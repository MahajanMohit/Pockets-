<?xml version="1.0" encoding="utf-8"?>
<!-- Fallback static layout for widget preview / API < 31 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FF000000"
    android:orientation="vertical"
    android:padding="12dp">

    <TextView
        android:id="@+id/widget_app_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="ZenDeck"
        android:textColor="#FF00897B"
        android:textSize="11sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/widget_title"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:ellipsize="end"
        android:maxLines="3"
        android:text="No links saved yet"
        android:textColor="#FFECEFF1"
        android:textSize="14sp"
        android:paddingTop="6dp" />

    <TextView
        android:id="@+id/widget_expiry"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text=""
        android:textColor="#FFF9A825"
        android:textSize="10sp" />

</LinearLayout>
